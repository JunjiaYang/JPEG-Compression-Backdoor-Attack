# Data Directory

This folder is a placeholder for raw and processed datasets. Populate it by running the download scripts or by mounting datasets locally. The contents are ignored by git to prevent committing large binaries.
