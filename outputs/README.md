# Outputs Directory

All generated artefacts such as checkpoints, logs, and figures should be written here. The folder is ignored by git except for this README and `.gitkeep` so that experiment results remain local.
